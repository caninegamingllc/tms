DATABASE_URL="file:./dev.db"
UPLOAD_DIR="./uploads"

# Set to "false" when the app is served over plain HTTP (no TLS).
# Leave unset on HTTPS production so secure session cookies are enabled.
# COOKIE_SECURE="false"

# Optional fallback provider if you later add Geoapify support in business search.
# GEOAPIFY_API_KEY=""
